# Supabase — Project Settings > API (https://supabase.com/dashboard/project/_/settings/api)

# Project URL, e.g. https://xxxxxxxxxxxx.supabase.co
SUPABASE_URL=

# "Publishable" key — safe to expose client-side, access is restricted by RLS policies
SUPABASE_PUBLISHABLE_KEY=

# "Secret" key — server-side only, bypasses RLS. Never expose to the browser.
SUPABASE_SECRET_KEY=

# JWKS URL, used to verify Supabase Auth JWTs server-side
SUPABASE_JWKS_URL=
